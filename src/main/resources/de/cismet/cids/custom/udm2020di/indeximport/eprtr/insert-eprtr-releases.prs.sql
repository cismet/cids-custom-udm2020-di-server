INSERT INTO EPRTR_RELEASE (
KEY,
NAME, 
INSTALLATION,
POLLUTANT, 
POLLUTANT_GROUP, 
RELEASE_TYPE,
NOTIFICATION_PERIOD,
MIN_DATE, 
MAX_DATE, 
VALUE,
SRC_RELEASE_ID, 
SRC_CONTENT)
VALUES(
:KEY,
:NAME, 
:INSTALLATION,
:POLLUTANT_ID, 
:POLLUTANT_GROUP_ID,
(SELECT ID FROM TAG WHERE TAG.KEY = :RELEASE_TYPE AND TAG.TAGGROUP_KEY='EPRTR.RELEASE_TYPE' AND ROWNUM = 1), 
(SELECT ID FROM TAG WHERE TAG.KEY = :NOTIFICATION_PERIOD AND TAG.TAGGROUP_KEY='EPRTR.NOTIFICATION_PERIOD' AND ROWNUM = 1),
:MIN_DATE,
:MAX_DATE, 
:VALUE,
:SRC_RELEASE_ID, 
:SRC_CONTENT)